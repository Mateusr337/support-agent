# Local frontend dev only (npm run dev outside Docker).
# Copy to frontend/.env — not used by docker compose.
# With compose: set VITE_API_URL in root .env; it is injected into the frontend container.

VITE_API_URL=http://localhost:8000
